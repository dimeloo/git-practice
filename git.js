// git commands

// git init = create resources to track changes
// git add . = adds all of the files in the project to be tracked
// git commit -m "began app setup" = similar to saving but only locally
// git push origin master = if you have a 'remote' host setup, this pushes the latest version to the remote host

// general workflow
// git add . = adds changes
// git commit - m 'some comment' = saves this version
// git push = uploads this version